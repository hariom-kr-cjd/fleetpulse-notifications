ktor {
    deployment {
        port = 3001
    }
}

mongo {
    uri = "mongodb://localhost:27017/fleetpulse-test"
}

auth {
    jwt_secret = "default-secret"
    internal_api_key = "default-key"
}

cors {
    allowed_origins = "http://localhost:4200"
}
